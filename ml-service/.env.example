PORT=8000
# Comma-separated; "*" allows all (server-to-server calls from Express don't need CORS).
ALLOWED_ORIGINS=*
# Daily data points required before a held-out test split is used for metrics.
MIN_TRAIN_POINTS=10
